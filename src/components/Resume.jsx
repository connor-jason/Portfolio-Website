import React from 'react';

const ResumeItem = () => {
    return (
        <div>
        </div>
    );
}

/**
 * Resume component for the website.
 * @returns {JSX.Element} The rendered Resume.
 */
const Resume = () => {
    return (
        <div>

        </div>
    );
};

export default Resume;
